[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
	name = leomrtinez
	email = martinez.leo.work@gmail.com

[http]
	version = HTTP/1.1
